**Hüküm: DÜZELTMEYLE KABUL.**
`V` = `viewer/template.source.html`; `T` = `archify/test/drilldown-nested-browser.test.mjs`.

- **P1-b AÇIK** (`V:6975`): torun handshake’i sürerken kökün `chainBelow` bilgisi eksik; iki seviyenin fallback’i yine 250 ms oluyor. Kaynak fonksiyonla bellek içi kontrolde, 10 ms istek gecikmesiyle kök **250 ms**, ara viewer **260 ms**’de `back()` çağırdı. İçten dışa sıra hâlâ garanti edilmiyor.
- **P2-a KAPANDI** (`V:6595`, `V:6827`; `T:596`): `(parent, component, child)` eşleşmesi ve uyumsuz hedefin yüklenmemesi doğrulanıyor.
- **P2-b KAPANDI** (`T:478`, `T:503`, `T:378`): bağımsız MutationObserver kayıtları ve pending-handshake session testi önceki kanıt açıklarını gideriyor. Yavaş-yanıt testi (`T:553`) yukarıdaki eksik-zincir durumunu kapsamıyor.
- **Yeni bağımsız regresyon/güvenlik açığı saptamadım.** Kalan düzeltme P1-b: kapanış sırası, eksik olabilen breadcrumb uzunluğuna dayanmamalı.
- İnceleme salt okunurdu; dosya yazma/git/ağ kullanılmadı. Bildirilen 31/31 yeniden çalıştırılmadı.