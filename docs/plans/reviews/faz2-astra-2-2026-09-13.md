**Hüküm: DÜZELTMEYLE KABUL.** Tur 1’in madde sırasını esas aldım.
`V` = `viewer/template.source.html`; `T` = `archify/test/drilldown-nested-browser.test.mjs`.

- **P1-a KAPANDI:** zorunlu session, ACK/crumb durum kapıları ve ascend yanıtında requestId eşleşmesi mevcut (`V:7022`, `V:7040`, `V:7059`, `V:7067`).
- **P1-b AÇIK:** eski timer’ın iptali düzelmiş (`V:6639`); fakat bağımsız 200 ms fallback’ler içten dışa kapanışı garantilemiyor (`V:6964`). Bellek içi kontrolde kök 200 ms’de, ara viewer 210 ms’de `back()` çağırdı.
- **P2-a AÇIK:** readiness/animasyon ayrımı ve derinlik kapısı düzelmiş (`V:6662`, `V:6877`); `row.child === childId` hâlâ denetlenmiyor (`V:6584`, `V:6821`). Bellek içi kontrolde kenarın hedefi `other` iken `wanted.html` stale olmadan yüklendi.
- **P2-b AÇIK — test kanıtı:** sıra testi, ebeveyn kaybolunca torunu da kaybolmuş saydığı için ters kapanışı yakalayamaz (`T:425`, `T:429`, `T:446`). Session’sız ACK yalnız zaten `open` durumunda sınanıyor (`T:337`, `T:342`).
- **P2-c AÇIK — Faz 5 öncesi:** gerçek torun klavye yönlendirmesi ve üç seviyeli `file://` kanıtı eksik (`T:14`, `T:182`); kök odak testi ilerleme sağlamış (`T:247`).

Yeni, bağımsız regresyon veya güvenlik açığı saptamadım; yukarıdaki iki doğruluk açığı Tur 1’den kalıyor ve kapanmalı.
**Faz 5 öncesi:** kalan klavye/file kanıtı; Faz 5’te bilinen üretilmiş dosyaların yenilenmesi ve kabul kapılarının tekrar çalıştırılması.
İnceleme salt okunurdu; yalnız kaynak fonksiyonlarıyla bellek içi kontroller yaptım. Bildirilen 1529/1521/3/5 ve 12 golden sonucunu yeniden koşturmadım.