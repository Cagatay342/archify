# Faz 5 brief — kapanış: "Faz 5 öncesi" boşluklar, üretilmiş dosyaların yenilenmesi, upstream hazırlığı (Sonnet)

Repo: /home/ubuntu/repos/archify, dal `feat/nested-drilldown` (Faz 1-3b commitsiz, astra KABUL). Önce oku: tüm astra incelemeleri `docs/plans/reviews/*.md` ("Faz 5 öncesi" / "AÇIK" maddeleri), raporlar `docs/plans/reports/faz{2,3a,3b}-*.md`, bakımcının şablon-sonrası yeniden üretim commit'i `git show --stat be5fd7c`, `archify/package.json` scripts, `scripts/build-zip.sh`, `archify/CONTRIBUTING.md`, `REVIEWING.md`, `docs/cases/archify-self/README.md`, `docs/decisions/identity-map-2026-09-09.md` (bakımcı non-goal'ları), plan `docs/plans/nested-drilldown-2026-09-12.md` §8 Faz 5.
KURALLAR: git commit/push/PR/issue yorumu/ağ YOK (upstream'e dokunmak kullanıcı onayı ister; sen yalnız dosya hazırlarsın). 2 denemede çözülmeyen → DUR, BLOKLAYAN. Rapor: `docs/plans/reports/faz5-release-prep-2026-09-13.md` (Türkçe ≤90 satır), iş ilerledikçe güncelle.

## A. "Faz 5 öncesi" boşlukları kapat (sıra: önce bunlar, çünkü viewer/test değişirse B tekrar gerekir)
1. Faz 2 (reviews/faz2-astra-2, -3): gerçek torun klavye kanıtı — CDP `Input.dispatchKeyEvent` öncesi `frame.contentWindow.focus()` + torun dokümanında bir düğüme `.focus()`, ardından `Runtime.evaluate` ile `document.activeElement`/`document.hasFocus()` doğrula; olmazsa `Page.bringToFront` ve `Emulation.setFocusEmulationEnabled` dene. Başarılıysa sentetik keydown testi gerçek tuşla değişsin; başarısızsa raporda kanıtlı "harness sınırı" + testte açık ad. `file://` üç seviye: Chrome `--allow-file-access-from-files` ile yerel dosyadan 3 seviye descend denemesi (yalnız kanıt/test; üretim davranışı değişmez).
2. Faz 3b (reviews/faz3b-astra-2): `Z` testinin gövdesi repeat/SELECT/[role=textbox] senaryolarını gerçekten sınasın; kilit testi iki korumayı (400 ms sessizlik, scale<2.5 rearm) bağımsız ayırt etsin; offline tercih (dive-pref) + yanlış-session reddi testi; export byte karşılaştırması (dive öncesi referans export ile birebir); ekran okuyucu: canlı bölgenin `aria-live` metin değişimini DOM'da doğrula, gerçek SR doğrulaması yapılamıyorsa "elle doğrulanmadı" diye belgele.
3. Faz 3a: `viewer-wheel-browser.test.mjs` yük altı flake — zamanlayıcı tabanlı `settle()` yerine `Archify.view.onChange` `transitioning:false` snapshot'ını bekleyen deterministik bekleme; 5 tekrar + `--test-concurrency=2` tam set içinde flake'siz kanıt.
4. Faz 4 UX notu (Fable): her iç seviye başlık/padding ile daralıyor; iç içe seviyelerde çocuk chrome'unu (başlık/nav) sıkıştıran CSS (`html[data-bundle-nested="true"]`) ile alan kaybını azalt — davranış değişmez, yalnız CSS; ekran görüntüsü kanıtı (3 seviye önce/sonra).

## B. Üretilmiş dosyaları bakımcı deseniyle yenile (be5fd7c ile aynı küme)
`npm run render:examples`, `npm run build:gallery`, `examples/checkout-platform-delta.html` + receipt (compare CLI, `architecture-delta.test.mjs` "checked-in Checkout compare artifact" testinin komutuyla), `docs/cases/archify-self` (`bundle` yeniden üret; README tarifi), `docs/cases/mco-runtime.*` ve `experiments/mco-showcase` (üretim komutunu repo içinde bul; bulamazsan raporda yaz, dokunma), `docs/assets/archify-live-proof.{gif,json}` (üretim scripti varsa çalıştır; yoksa dokunma ve raporda yaz), `archify.zip` (`scripts/build-zip.sh`, Node 22, deterministik). SONRA tam `npm test` (archify/ içinde; `ARCHIFY_CHROME=/usr/bin/google-chrome`): tüm kapılar (`check:viewer`, `check:brand-marks`, `check:validators`, `check:release-identity`, golden, run-tests) YEŞİL olmalı; yalnız Faz 0'ın bilinen-kötü 2 dosyası (`motion-governor-browser` bu hostta hang, `update-notifier` zamanlama) hariç tutulabilir (run-tests.mjs bunları nasıl seçiyor, gerekirse aynı grep-v listesiyle elle koş ve raporda yaz). Log: `docs/plans/reports/faz5-npm-test.log`. Bilinen fail KALMAMALI (ZIP tazelik dahil).

## C. Upstream hazırlığı (yalnız dosya; PR/yorum AÇMA)
`docs/plans/upstream/` altında: (1) `pr1-recursive-bundles.md` — draft PR gövdesi: motivasyon (#280, #367 "No third level" non-goal'unu yeniden değerlendirme talebi), kapsam (Faz 1+2: manifest N-derinlik ≤8, ağaç doğrulama, sahiplik sidecar zinciri, viewer özyinelemeli descend/breadcrumb/Esc, mesaj sözleşmesi session/requestId), geriye uyumluluk (2 seviye paketler değişmeden geçer; referans manifest byte kanıtı), testler, bilinen sınırlar; (2) `pr2-opt-in-camera-and-dive.md` — Faz 3a (wheel/pinch/onChange/gesture) + Faz 3b (varsayılan KAPALI, önizlemeli, iptal edilebilir alternatif tetik; bakımcının kapsam kararına saygı, ayrı PR); (3) `issue-280-comment.md` — kısa yorum taslağı; (4) `commit-plan.md` — önerilen mantıksal commit listesi (dosya grupları; Faz sırası; üretilmiş dosyalar ayrı "chore: rebuild generated artifacts" commit'i) — commit'i Fable/kullanıcı yapar. İngilizce yaz (upstream dili), REVIEWING.md/CONTRIBUTING.md beklentilerine uy.

## D. Kabul
A maddeleri hedefli testlerle; B tam `npm test` yeşil; C dosyalar hazır. Raporda: madde madde durum, yeniden üretilen dosya listesi (git status --short sayısı), npm test sayıları, BLOKLAYAN'lar, commit-plan özeti.
