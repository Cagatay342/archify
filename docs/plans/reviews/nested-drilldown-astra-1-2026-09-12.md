- **Hüküm: DÜZELTMEYLE UYGUN.** Yaklaşım uygulanabilir; ancak “handshake, Esc ve export aynen kalır” varsayımı geçersiz, N-derinlik için gezinme protokolü ve üretim hattı birlikte genişletilmeli.
- İnceleme salt okunur yapıldı; dosya yazmadım, git/ağ kullanmadım, test çalıştırmadım. Node sürümü ve self-map ownership SHA uyuşmazlığı doğrulandı; PR durumları ve test toplamları doğrulanmadı.

- **1) YANLIŞ — Derinlik kilidi yalnız `descend()` değil.** Passport, mark tıklaması ve listener kurulumu da çocukları engelliyor: `viewer/template.source.html:6546`, `:6781`, `:6803`, `:6806`.
- **YANLIŞ — Kamera katmanları yalnız scale’e bağlı değil.** `semantic` modu scale’den bağımsız `full` döndürüyor; manuel zoom 1–3’e sıkıştırıldığından `<1 → map` normal manuel akışta erişilmiyor. `viewer/viewer-camera.js:67`, `:242`.
- **YANLIŞ — “Her viewer ~300 KB” düşük tahmin.** Mevcut üretilmiş template 806.367 bayt; self-map HTML’leri yaklaşık 827–836 KB. Üretim hedefi: `scripts/generate-viewer.mjs:8`.
- **YANLIŞ — Manifest byte-aynılığı tüm fazlarda korunamaz.** Bundle her HTML’i yeniden render edip `artifact_sha256` hesaplıyor; viewer değişince manifest hash’leri değişir. Byte-aynılık kabulünü Faz 1’de, aynı renderer/template girdileriyle sınırla. `archify/bundle/diagram-bundle.mjs:290`, `:311`.

- **2) EKSİK — Handshake özyinelemesi çalışmaz.** `nestedChild()` dalı yalnız `window.parent` mesajlarını kabul edip dönüyor; ara viewer torunun ACK/Esc mesajını reddeder. Üst ebeveyn ve doğrudan çocuk için ayrı alıcı yolları gerekir. `viewer/template.source.html:6747`, `:6765`, `:6767`.
- **EKSİK — Subtree sonradan gelince listener kurulmaz.** Başlangıçta manifest bulunmayan çocukta mark ve iframe `load → postHello` bağlantıları yoktur. Manifest kabulünden sonra tekrar çağrılabilen, çift kayıt yapmayan aktivasyon gerekir. `viewer/template.source.html:6801`.
- **EKSİK — Subtree sözleşmesi yetersiz.** `{diagrams, drilldowns}` yanında mevcut diyagram kimliği, mutlak derinlik ve `max_depth` gerekli. `drillFor()` yalnız component ID arıyor; farklı seviyelerde aynı ID yanlış satıra bağlanabilir. `(parent, component, child)` eşleşmesini doğrula. `viewer/template.source.html:6481`, `:6657`.
- **EKSİK — Gezinme oturumu/readiness ayrımı yok.** `level1`, ACK gelmeden 170 ms sonra da yazılıyor; bunu hazır sinyali yapma. Mesajlara gezinme kimliği ekle; eski ACK, timeout, crumb ve gecikmiş `reveal()` işlemlerini iptal et. `viewer/template.source.html:6705`, `:6713`, `:6741`.
- **EKSİK — Esc yönü ve odak.** Mevcut forward alıcısı doğrudan `back()` çağırıyor; tuşun kök breadcrumb’da olduğu durumda en iç seviyeye yönlendirme yok. Bir Esc yalnız bir geçici durumu veya seviyeyi kapatmalı; dönüşte odağı ilgili parent node’a taşımalı. `viewer/template.source.html:6775`, `:6923`, `:6929`.
- **EKSİK — CSS/local-global level ayrımı.** Host yalnız `level="1"` için açılıyor; yalnız entry SVG gizleniyor. Çocuk toolbar/nav’ı da gizli. Yerel “çocuğum açık” durumunu mutlak derinlikten ayır; ara breadcrumb/silhouette’leri kaldırıp görünür geri/zoom kontrollerini tanımla. `viewer/template.source.html:4400`, `:4408`, `:4457`.
- **EKSİK — İki üretim çıktısı var.** Şema değişikliği `generated-validators.mjs` üretimini gerektirir; validator doğrudan buradan import ediliyor. Faz 3 kamera değişikliği de yeniden `generate:viewer` gerektirir. `archify/bundle/diagram-bundle.mjs:9`; `archify/scripts/generate-validators.mjs:12`; `scripts/generate-viewer.mjs:13`.
- **EKSİK — Dinamik şema sınırı.** Standart JSON Schema’da `maximum: max_depth-1` doğrudan ifade edilmez; mevcut AJV kurulumunda böyle bir mekanizma yok. Şemada sabit tavan, graph check’inde gerçek derinlik/level tutarlılığı kullan. `archify/scripts/generate-validators.mjs:15`; `archify/schemas/bundle.schema.json:26`.
- **EKSİK — Export cleanup drilldown state’ini temizlemiyor.** Bugün state HTML üzerinde olduğundan SVG klonuna girmez; bu tesadüfi sınırı korumak gerekir. Yeni SVG attribute/overlay eklenirse cleanup ve temizliğin doğrulaması genişletilmeli; kalıcı drilldown markları korunmalı. `viewer/export.js:232`; `viewer/export-cleanup.js:1`, `:164`.
- **EKSİK — Node-cap iki yerde.** `node_count >12` önce şemada reddedilebilir; mutlaka `bundle/node-cap` alınacağı varsayılmamalı. L0 sadeleştirmesinde taşınan node’ların edge uçları ve ownership bağları da güncellenmeli. `archify/schemas/bundle.schema.json:27`; `archify/bundle/diagram-bundle.mjs:407`, `:515`.
- **EKSİK — Locate/ownership de iki seviyeli.** `locate --bundle`, bütün linklerin parent’ını entry kabul ediyor; torunda açıkça hata verir. Ownership altküme kontrolü ve locate projection dağıtımı da her doğrudan ebeveyn–çocuk bağlantısına genişletilmeli. `archify/locate/cli.mjs:344`; `archify/bundle/diagram-bundle.mjs:559`; `viewer/template.source.html:6645`.
- **EKSİK — Uyumluluk ve test kapsamı.** Mevcut check aynı çocuğun farklı component’lerden hedeflenmesini yasaklamıyor; yeni ağaç şartı kabul edilen bundle’ları daraltır. Ayrıca Chrome testleri varsayılan skip olabilir; gerçek iframe kaynaklarıyla kardeş geçişi, torun stale, breadcrumb odaklı Esc ve export testleri zorunlu olsun. `archify/bundle/diagram-bundle.mjs:504`; `archify/test/drilldown-browser.test.mjs:7`.

- **3) RİSK sırası — P0: Protokol/yaşam döngüsü.** Önce iki yönlü mesaj sözleşmesi, açık readiness ve oturum iptali; sonra üç seviyeli tarayıcı testi.
- **P0: Üretim/doğrulama uyumsuzluğu.** Validator üretimini Faz 1’e, viewer üretimini Faz 2 ve 3’e ekle; manifest–spec–SVG bağlantılarını her kenarda doğrula.
- **P1: Geri dönüş ve istemsiz yeniden dalış.** Ascend sonrası yüksek zoom korunur; yeniden tetiklenmeyi yeni kullanıcı hareketine kadar kilitle, wheel momentumunu ayrı jest sayma.
- **P2: Kaynak tüketimi ve upstream uyumluluğu.** Derinlik 8, toplam boyutu sınırlamaz; toplam diyagram/manifest bayt bütçesi koy. “Sınırsız” hedefini kaldır; ağaç şartı ve bakımcının “No third level” kararındaki değişikliği açıkça belgele.

- **4) İTİRAZ — Varsayılan açık 2.5 + 250 ms dalışa itirazım var.** Okumak için zoom yapmak gezinme niyeti değildir; mutlak scale node’un ekrandaki büyüklüğünü de temsil etmez.
- Önerim: zoom yalnız hedef node’da görünür **“İçini aç”** kontrolünü belirginleştirsin; tıklama/Enter dalsın. Otomatik seçenek istenirse açık tercih, görünür hedef önizlemesi ve yeni bir zoom jesti gerektirsin.
- Ayrıca mevcut kamera wheel/pinch zoom uygulamıyor; buton/klavye zoom ve pointer-pan var. “İkinci wheel-out” için ayrı jest tanıma gerekir; minimumda scale değişmediğinden yalnız `onChange` yeterli olmaz. `viewer/viewer-camera.js:243`, `:481`, `:516`.