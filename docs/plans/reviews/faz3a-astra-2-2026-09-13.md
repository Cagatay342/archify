**Hüküm: DÜZELTMEYLE KABUL. Faz 3b öncesinde açık P1’ler giderilmeli.**

- **P1 mobil pinch — KAPANDI:** başlangıç ve güncellemede mobil-wide koruması var (`viewer/viewer-camera.js:574`, `:599`).
- **P1 pointer sahipliği — AÇIK:** pinch capture/üçüncü parmak düzeltildi; ancak `lostpointercapture` drag’i temizlemiyor. Capture kaybından sonra mouse hareketi pan yapmayı sürdürüyor (`viewer/viewer-camera.js:689`, `:667`).
- **P1 jest sözleşmesi — AÇIK / yeni regresyon:** minimumda parmakları yeniden açmak ve üçüncü parmağı oynatmak da `minZoomOut` üretiyor; kontrol anlık mesafe değişimini kullanmıyor (`viewer/viewer-camera.js:608`, `:685`).
- **P2 svgOrigin scroll — KAPANDI:** container iç scroll’u çıkarılıyor (`viewer/viewer-camera.js:309`); RTL/sol scrollbar doğrulaması eksik.
- **P2 kök-neden açıklaması — KAPANDI:** açık düzeltme mevcut; eski yanlış cümle hâlâ raporun başında (`docs/plans/reports/faz3a-camera-gestures-2026-09-13.md:118`, `:13`).
- **P2 testler — AÇIK:** kapsam genişlemiş; capture kaybı, dışarıda bırakma, ters pinch yönü, RTL/resize eksik. Dosyada **19 alt-test + üst test** var (`archify/test/viewer-wheel-browser.test.mjs:301`, `:377`, `:460`).
- **P2 export byte-aynılığı — KAPANDI:** wheel+pinch sonrası ham eşitlik testi eklenmiş (`archify/test/viewer-wheel-browser.test.mjs:443`).

**Faz 3b için henüz yeterli değil:** ≥2.5, yerleşme sonrası 250 ms görünür önizleme ve jest kimliğiyle yeniden-dalış kilidi kurulabilir; fakat yanlış `minZoomOut` olayları çocukta 2× sayacını doldurup istemsiz çıkış üretebilir. Kimlik `(source,id)` olarak ele alınmalı.

Salt okunur bellek testinde ters yönde **2**, üçüncü parmak hareketinde **1** yanlış olay ve capture kaybından sonra pan doğrulandı. Chrome/59–59 koşusunu yeniden çalıştırmadım; dosya yazma, git ve ağ kullanılmadı.