**Hüküm: KABUL.** Yeni doğruluk/güvenlik regresyonu saptamadım.

- **P1-1 KAPANDI:** taze durum/hedef doğrulaması, semantic/mod/medya değişiminde iptal ve pointerleave/blur mevcut (`viewer/dive.js:206`, `:220`, `:315`).
- **P1-2 KAPANDI:** ascend saati sıfırlıyor; her jest bildirimi saati güncelliyor; kilit ve `scale<2.5` rearm ayrı koşullar (`viewer/dive.js:219`, `:273`, `:283`).
- **Kilit yeterli:** mevcut kamera olay sözleşmesinde devam eden akış 400 ms sessizlik sağlayamaz; sessizlik tek başına da yeniden dalışı açmaz. Bu bir fiziksel-jest kimliği garantisi değil, yeterli bir davranış sözleşmesidir.
- **P1-3 KAPANDI:** yaprakta `escapeCapable` bağımsız; çift farklı jestle çıkış aboneliği korunuyor (`viewer/dive.js:39`, `:239`, `:309`).
- **P2 export KAPANDI:** önizleme seçicisi CSS toplayıcısının dışında (`viewer/template.source.html:4426`; `viewer/export.js:269`).
- **P2 tercih KAPANDI:** ACK sonrası ve değişiklikte doğrudan çocuğa aktarılıyor (`viewer/dive.js:88`, `:290`). `dive-pref`, yalnız doğrudan ebeveynden ve eşleşen sayısal session ile kabul ediliyor; **Faz 2 kaynak/session kurallarına uyumlu** (`viewer/template.source.html:7114`, `:7178`).
- **P2 a11y/Z kod düzeltmeleri KAPANDI:** kalıcı canlı bölge, düğüme göre konum, repeat/SELECT/textbox koruması mevcut (`viewer/template.source.html:5655`, `:7312`; `viewer/dive.js:142`).
- **P2 test/flake kanıtı AÇIK — Faz 5 öncesi:** Z testinin adı repeat/SELECT/textbox dese de gövdesi bunları sınamıyor (`archify/test/drilldown-dive-browser.test.mjs:440`). Kilit testi iki korumayı bağımsız ayırt etmiyor (`:394`). Offline tercih/yanlış-session, export byte karşılaştırması ve ekran okuyucu doğrulaması tamamlanmalı.
- **Infinity regresyonu KAPANDI:** sonlu fallback ve üretilmiş şablon guard’ı mevcut (`viewer/dive.js:273`; `archify/test/generate-viewer.test.mjs:143`).

Salt okunur `--check`, non-finite taraması ve bellekte dwell/yaprak/kilit kontrollerim geçti; sessizlik ile rearm’ı bağımsız sınadım. Chrome/kabul setini tekrarlamadım; **1602/1592 ve flake açıklaması uygulayıcı raporunun kanıtıdır**.