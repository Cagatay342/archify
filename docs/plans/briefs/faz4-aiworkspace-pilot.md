# Faz 4 brief — AIWorkspace pilotu: iç içe (3 seviye) diyagram paketi (Sonnet)

İki repo: fork `/home/ubuntu/repos/archify` (dal `feat/nested-drilldown`; Faz 1-3b commitsiz hazır; BU FAZDA FORK'A DOKUNMA — bug bulursan raporda BLOKLAYAN/istek yaz, yamalama) ve hedef `/home/ubuntu/repos/AIWorkspace` (dal `main`; başka bir oturumun commitsiz dosyaları var: `agent-runners/…`, `scripts/model-bench/…`, `docs/research/…` — onlara DOKUNMA, stash/checkout/reset YOK, commit YOK).
Önce oku: `AIWorkspace/docs/diagrams/README.md`, 6 spec (`docs/diagrams/*.json`), fork `archify/references/drilldown-bundles.md` (tamamı; "Adding a drilldown child", manifest, flat dizin kuralı, 12 düğüm tavanı, `max_depth`), `docs/cases/archify-self/README.md` (bundle tarifi), `docs/plans/reports/faz2-viewer-recursive-2026-09-12.md` ve `faz3b-zoom-dive-2026-09-13.md` (okuyucu davranışı), `archify/test/drilldown-nested-browser.test.mjs` (Chrome ile 3 seviye kanıt deseni, yerel HTTP sunucu). CLI: `node /home/ubuntu/repos/archify/archify/bin/archify.mjs` (Node 22). Kurulu skill (`~/.claude/skills/archify`, 2.16) `drilldown` alanını TANIMAZ → kanonik spec'lere `drilldown` EKLENMEZ.

## Keşif (Fable)
- L0 `aiworkspace-runtime.architecture.json` 14 bileşen: users, gateway, n8n, plane, engine, cli, github, litellm, providers, data, mcpo, codeexec, ui, obs → paket tavanı 12 (entry dahil, `bundle/node-cap`).
- L1: crewai-engine-ic-mimari (12), model-ve-arac-katmani (12), bilgi-ve-veri-katmani (12), arayuz-otomasyon-gozlem (11), orkestrasyon-kosusu.workflow (11).
- Paket kimliği = dosya kökü, `^[a-zA-Z][a-zA-Z0-9_-]*$` → `aiworkspace-runtime.architecture.json` geçersiz; paket kopyaları yeni köklerle üretilir.
- Paket düz dizindir; `<id>.json` + `<id>.html` yan yana; entry = drilldown beyan eden ve hedef olmayan tek diyagram.

## Tasarım (uygulanacak)
1. **Türetilmiş paket dizini** `AIWorkspace/docs/diagrams/nested/` (yeni). Kanonik spec'ler kaynak-of-truth kalır; paket bir üretim scriptiyle türetilir: `AIWorkspace/scripts/diagrams-nested.mjs` (Node, bağımlılıksız). Script: (a) kanonikleri okur, (b) dönüşüm tablosunu uygular, (c) `nested/<id>.json` yazar, (d) her biri için fork CLI `deliver <tip> nested/<id>.json nested/<id>.html --quality showcase --repo-root <AIWorkspace>` (workflow için `deliver workflow …`, `--repo-root` yalnız architecture'da — README kuralı), (e) `bundle nested`, (f) `bundle nested --check`, (g) `visual-check nested/<entry>.html` (+ çocuklar) → PASS şart. `ARCHIFY_BIN` env ile CLI yolu ayarlanabilir (varsayılan fork yolu). Script idempotent; `--check` modu yalnız doğrular.
2. **Dönüşüm tablosu** (script içinde açık, yorumlu):
   - Kökler: `aiworkspace-runtime` (entry, architecture), `crewai-engine` ← crewai-engine-ic-mimari, `orkestrasyon-kosusu` ← orkestrasyon-kosusu.workflow, `model-ve-arac` ← model-ve-arac-katmani, `bilgi-ve-veri` ← bilgi-ve-veri-katmani, `arayuz-otomasyon-gozlem` ← aynı.
   - L0 sadeleştirme 14→12: yalnız L1'de zaten detaylanan çiftleri birleştir: `providers` → `litellm` içine (label "LiteLLM → sağlayıcılar"; sources iki bileşenin birleşimi; providers'a giden connection'lar litellm'e yönlenir), `codeexec` → `mcpo` içine (label "mcpo + code-executor"; aynı kural). Bağlantı/boundary/card referansları güncellenir; sonuç `validate architecture … --repo-root` geçer. Başka birleşme yapma; gerekirse raporda öner.
   - Drilldown kenarları (`components[].drilldown = "<id>"`, bileşen başına en fazla 1): L0 `engine` → `crewai-engine`; L0 `litellm` → `model-ve-arac`; L0 `data` → `bilgi-ve-veri`; L0 `ui` → `arayuz-otomasyon-gozlem`; L1 `crewai-engine.core` → `orkestrasyon-kosusu` (workflow çocuk, seviye 2 → `max_depth` 3). Kenar etiketi (`label`) manifestte otomatik; drilldown mark'ı renderer çizer.
   - Ownership sidecar'ları bu pilotta YOK (locate ayrı tur; raporda "sonraki iş").
3. **Belgeler**: `docs/diagrams/README.md`'ye "İç içe paket (pilot)" bölümü: üretim komutu `node scripts/diagrams-nested.mjs`, ne zaman yeniden üretilir (kanonik spec değişince, aynı turda), okuyucu davranışı (mark tıkla / Passport "Descend" / Esc ile çık; `Z` ile opt-in zoom-dalış), fork CLI bağımlılığı (kurulu skill'e taşınana kadar). `docs/diagrams/index.html`'e paket girişi kartı (`nested/aiworkspace-runtime.html`). `.gitignore`'a visual-check artıkları zaten kapsanıyor mu kontrol et.
4. **Kanıt**: (a) `bundle --check` çıktısı; (b) `visual-check` PASS ölçümleri; (c) gerçek Chrome (headless, `/usr/bin/google-chrome`, fork testlerindeki CDP yardımcı deseni; yerel HTTP sunucu) ile `aiworkspace-runtime` → engine → `crewai-engine` → core → `orkestrasyon-kosusu` 3 seviye descend (`data-drilldown-state="open"` ACK), kök breadcrumb metni, Esc ×2 ile çıkış; `Z` + wheel ile opt-in dalış bir kez; her adımda PNG ekran görüntüsü `docs/diagrams/nested/.evidence/` (gitignore'lu) — raporda yollar. Bu kanıt scripti tek seferlik, `scratchpad/f4/` altında kalsın (AIWorkspace'e test eklenmez).
5. **Kısıtlar**: kanonik `docs/diagrams/*.json|*.html` DEĞİŞMEZ; fork DEĞİŞMEZ; commit yok; 2 denemede çözülmeyen → DUR, BLOKLAYAN. Fork'ta ürün hatası bulursan (ör. workflow çocukta mark, `--repo-root` ile bundle uyumsuzluğu) BLOKLAYAN olarak yaz, Fable karar verir.

## Rapor
`/home/ubuntu/repos/archify/docs/plans/reports/faz4-aiworkspace-pilot-2026-09-13.md` (Türkçe ≤80 satır): dönüşüm tablosu kararları, node sayıları, manifest özeti (max_depth, kenarlar), bundle --check / visual-check sonuçları, Chrome kanıt adımları + PNG yolları, AIWorkspace'te değişen/eklenen dosya listesi, BLOKLAYAN'lar, "sonraki iş" (ownership/locate, kurulu skill'e taşıma, L0 birleşmelerinin kanonike geri taşınıp taşınmayacağı).
