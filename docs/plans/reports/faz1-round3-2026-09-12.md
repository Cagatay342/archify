# Faz 1 — Tur 3 raporu: sahiplik sidecar sertleştirme (2026-09-12)

Repo `/home/ubuntu/repos/archify`, dal `feat/nested-drilldown`. Commit yok, stash/checkout/reset yok.
`viewer/`, `archify/assets/template.html`, `archify.zip`, `docs/gallery`, `docs/cases`'a dokunulmadı.
Kaynak: `docs/plans/reviews/faz1-astra-3-2026-09-12.md` (çözüm tasarımı), `docs/plans/reviews/faz1-astra-2-2026-09-12.md` (açık maddeler tur-2), önceki rapor `docs/plans/reports/faz1-bundle-depth-2026-09-12.md`.

## Madde madde durum

**1. Kimlik dosyadan — KAPANDI.**
`validateOwnershipSubset(manifest, sidecar, parentSidecar, bundleDir, {sidecarPath, parentSidecarPath, ownSpecPath, parentSpecPath})` — `archify/bundle/diagram-bundle.mjs:121-215`. `sidecarPath` zorunlu (`:133`, eksikse `TypeError`). Kimlik artık `ownershipSpecPath()` (`:109-112`, `x.ownership.json → x.json`) ile okunan dosyadan türetilip `diagramIdForSpecPath()` (`:114-119`) ile `manifest.diagrams[].file`e eşleniyor; `sidecar.map`den beyana dayalı eski `ownId` fallback'i tamamen kalktı. `resolve(dirname(sidecarPath), sidecar.map)` beklenen spec yoluna eşit değilse veya diagram eşleşmiyorsa `bundle/ownership-not-subset` (`:143-147`). Basename-only karşılaştırma kalmadı — hem bu kontrol hem `parentLink.map` eşleşmesi artık tam `path.resolve` kullanıyor.
Sapma: caller'lar (root ve child_map yürüyüşü) `ownSpecPath`/`parentSpecPath`'i EXPLICIT geçiyor (aşağıda madde 2), sade dosya-adı-kuralına içsel fallback yalnız saf-birim-test/kök-map-yok senaryosunda devreye giriyor — gerekçe: `manifest.ownership.file` keyfi isimli bir dosyaya bağlanabiliyor (`archify/test/locate-bundle-delivery.test.mjs:179` mevcut testi), bu yüzden kök sidecar'ın kimliği dosya adı kuralından değil kendi `map` beyanından (yine de `manifest.diagrams[]`e karşı doğrulanarak) türetiliyor; astra tasarımı bunu ayrık ele almamıştı, mevcut bir testi kırmadan kapatmak için gerekliydi.
Test: `archify/test/bundle-depth.test.mjs:273-303` ("rejects a sidecar whose declared map does not match its own file identity" — a→x, b→y kenarları, x.ownership.json içinde `map:'y.json', parent:'b'` → RET, dijital/embed karışmasın diye doğrudan `validateOwnershipSubset` birim çağrısı, brief'teki "hedef kodla RET" şartını karşılıyor).

**2. Gerçek ebeveyn bağı — KAPANDI.**
`walkOwnershipTree(current, currentPath, currentSpecPath)` (`:776`) her çocuğa `{sidecarPath: childOwnPath, parentSidecarPath: currentPath, ownSpecPath: childSpecPath, parentSpecPath: currentSpecPath}` geçiriyor (`:814-819`). `childSpecPath = resolve(dirname(currentPath), component.child_map)` (`:813`) — çocuğun beklenen kimliği, gezilen `component.child_map` işaretçisinin kendisi; `parentSpecPath` bir önceki seviyede doğrulanmış `currentSpecPath`. `validateOwnershipSubset` içinde `parentSidecarPath` verildiğinde artık gerçek-yürüyüş dalı çalışıyor (`:155-172`): `parent.map` tam olarak `parentSpecPath`e eşit olmalı (manifestteki HERHANGİ bir uygun satır değil), ve `(realParentId, component, ownId)` üçlüsü `manifest.drilldowns`te olmalı. `child_map` yolları `dirname(currentPath)` tabanında çözülüyor (`:782`, root için `archify/locate/ownership.mjs`'in kendi kuralıyla aynı). `loadSiblingSidecar` artık `{data, path}` döndürüyor (`:512-524`) ve kök çağrısı kendi `parentSidecarPath`'ini bu gerçek yoldan alıyor (`:764`), yani kökün kendi `parent`/`map`'i de aynı kuralla doğrulanıyor.
Test: `archify/test/bundle-depth.test.mjs:425-497` — checkout-platform'un GERÇEK `queue→ledger-flow` kenarı dururken, `payments` sidecar'ına aynı `queue` adını taşıyan (fakat farklı diyagramdan) bir sahte `child_map:'ledger-flow.json'` ekleniyor; ledger-flow'un kendi `parent.component='queue'` beyanı HER İKİ yürüyüşte de isim eşleşmesini geçiyor (eski `component.id` kontrolünün kör noktası), yalnız yeni `parentSpecPath` kontrolü sahte yolu (payments ≠ checkout-platform) yakalayıp reddediyor; globlar kasıtlı boş bırakılarak başka kontrol tetiklenmesi önlendi.

**3. Şema hatası dalı keser — KAPANDI.**
`validateOwnershipSubset` şema hatalarını hesapladıktan hemen sonra `return failures` (`:126-131`) — subset/glob mantığına hiç girmiyor. `walkOwnershipTree` içinde çocuk çağrısının döndürdüğü `childFailures` boş değilse özyineleme başlatılmıyor, `continue` (`:824`) — brief'in "kaydet+continue, özyineleme başlatma" şartı. (İlk denemede yalnız `validateOwnershipSubset`i erken döndürmüştüm; test hâlâ `TypeError: Cannot read properties of null (reading 'child_map')` verdi çünkü `walkOwnershipTree` yine de malformed `childSidecar`ın `components`ine özyinelemeyle giriyordu — ikinci düzeltme bu `continue`ydu, 2 deneme içinde kapandı.)
Test: `archify/test/bundle-depth.test.mjs:498-552` — çocuk sidecar `components:[null]`, geçerli `parent` bağı → `BundleError` (`bundle/ownership-not-subset`), `TypeError` değil. Mevcut "ambiguous" testi güncellendi (`:253-271`): eksik `parent.map` artık şemada durduğu için (`archify/schemas/ownership.schema.json:18`) eski "ambiguous" mesajı yerine şema-mesajı bekleniyor.

**4. Kök literal null — KAPANDI.**
`JSON.parse` `catch` dalı `note()` çağırıp hemen `return` ediyor (`:738-743`); ardından kökte null/nesne/dizi kontrolü de ayrıca `return` ediyor (`:744-747`) — her ikisi de sibling yükleme ve şema kontrolünden ÖNCE. Eski `if (!sidecar) return;` (sessiz, kayıtsız dönüş) tamamen kaldırıldı; onun ALTINDAKİ "artık ulaşılamaz" ikinci kontrol de kaldırıldı (dead code'du).
Test: `archify/test/bundle-depth.test.mjs:553-573` — manifestteki kök sidecar dosyası `null\n` (SHA ve embed güncel) → `bundle/ownership-parse`.

**5. DFS sözleşmesi — KAPANDI (kod değişmedi, doküman netleşti).**
`buildTree` (DFS, açık yığın) dokunulmadı. `archify/references/drilldown-bundles.md`e madde 2'nin sonuna cümle eklendi: geçerli ağaçta `level = ebeveyn.level + 1` (BFS ile aynı sonucu verir, tek yol olduğu için); geçersiz grafikte (shared/cycle, ikisi de zaten reddediliyor) `level` ilk keşif değeridir, en kısa yol değil. Test zaten mevcuttu (`a→b→c, a→c` → `c.level===2`, `bundle/drilldown-shared`), dokunulmadı.

**6. Byte-aynılık kanıtı (#7) — KAPANDI.**
Referans, DEĞİŞİKLİK ÖNCESİ (commit `cc1b33a`, brief'teki temiz worktree `scratchpad/archify-idmap`, salt-okunur) kodla üretildi: `node bin/archify.mjs render ...` + `node bin/archify.mjs bundle <dir> --json` orijinal `test/fixtures/bundle-checkout` (checkout-platform/ledger-flow/payments, deep DEĞİL) fixture'ı üzerinde, çıktı `/tmp/.../scratchpad/f1r3/old-shallow/manifest.json`e yazıldı (worktree'ye hiçbir dosya yazılmadı; `node_modules` yoktu ama runtime hiçbir devDependency import etmiyor, yalnız `generate-validators.mjs` script'i ajv kullanıyor). Bu bayt dizisi `archify/test/fixtures/bundle-checkout/manifest.reference.json` olarak asıl repoya kopyalandı. `archify/test/bundle-depth.test.mjs:53-81` artık YENİ kodla üretilen manifest'i bu referansla `Buffer.compare(...)===0` ile karşılaştırıyor — digest/boşluk/sıra maskeleme yok, alan-alan pinleme kaldırıldı. Sonuç: **byte-for-byte eşit**, ortam bağımlı fark bulunmadı (mutlak yol/zaman alanı manifest'te yok), maskeleme eklenmedi.

## Sapmalar (özet)
- Madde 1/2'nin tasarımı kökün kimliğini "entry" olarak sabitlemiyordu; ben önce öyle denedim (`entry`'nin spec yolu), bu `locate-bundle-delivery.test.mjs:179` ("non-default ownership file explicitly bound by the manifest") testini kırdı çünkü o test `manifest.ownership.file`i entry-DIŞI bir diyagramın (`payments`) sidecar'ına bağlıyor. Kökün bağımsız bir işaretçisi olmadığından (child_map gibi), kimliğini kendi `map` beyanından alıp yalnız `manifest.diagrams[]`e karşı gerçeklik doğrulaması yapacak şekilde düzelttim (`:761`). Astra tasarımı bunu ayrıştırmamıştı; mevcut bir testi kırmadan brief'in "Başka fail = düzelt" şartını karşılamak için gerekliydi.
- `archify/test/bundle-ownership.test.mjs` brief'in dosya listesinde yoktu ama yeni zorunlu `sidecarPath` parametresi onun 3 doğrudan `validateOwnershipSubset` çağrısını kırdı (`bundle/ownership-not-subset` yerine `TypeError`); `FAKE_BUNDLE_DIR`/`CHILD_SIDECAR_PATH` sabitleri eklenip çağrılar güncellendi, `manifest.diagrams`e `payments` eklendi — davranış değişmedi, yalnız yeni imza karşılandı.

## BLOKLAYAN
Yok.

## Kabul seti (`docs/plans/reports/faz1-round3-tests.log`)
`cd archify && npm run check:viewer && npm run check:validators && node test/golden.mjs && ARCHIFY_CHROME=/usr/bin/google-chrome node --test --test-concurrency=2 $(ls test/*.test.mjs | grep -v -E "motion-governor-browser|update-notifier")`
(npm komutları `archify/` alt dizininden koşuldu — `package.json` orada; `archify/CONTRIBUTING.md:56` de aynı şekilde `cd archify` diyor.)

- `check:viewer` exit=0, `check:validators` exit=0, `node test/golden.mjs` exit=0 (tüm alt kontroller ok).
- `node --test`: **1513 test / 1507 pass / 1 fail / 5 skip**, exit=1.
- Tek fail (beklenen, bilinen): `archive build is byte-for-byte reproducible across caller time zones without system zip` (`archify/test/release-package-gates.test.mjs:15`) — ZIP tazelik, bu fazın kapsamı dışı.
- Hedefli tur (bu değişikliğin dokunduğu dosyalar): `node --test test/bundle-*.test.mjs test/locate-bundle*.test.mjs test/generate-validators.test.mjs` → 74 test / 74 pass / 0 fail.
- `test/bundle-depth.test.mjs` tek başına → 22 test / 22 pass / 0 fail.

## Değişen dosyalar
- `archify/bundle/diagram-bundle.mjs` — `validateOwnershipSubset` yeniden yazıldı (kimlik+gerçek-ebeveyn+erken-dönüş), `loadSiblingSidecar` yol döndürüyor, `ownership` check bloğu (kök null, `walkOwnershipTree` currentSpecPath zinciri, child_map `dirname(currentPath)` tabanlı çözüm, schema-fail sonrası `continue`).
- `archify/test/bundle-depth.test.mjs` — byte-aynılık testi referans dosyasına bağlandı; item 1/2/3/4 için testler eklendi/güncellendi (bkz. madde madde durum).
- `archify/test/bundle-ownership.test.mjs` — yeni zorunlu `sidecarPath` parametresi için 3 çağrı güncellendi, `manifest.diagrams`e `payments` eklendi.
- `archify/test/fixtures/bundle-checkout/manifest.reference.json` — yeni, commit `cc1b33a`'dan üretilmiş referans manifest.
- `archify/references/drilldown-bundles.md` — DFS/level sözleşme cümlesi.
- `CHANGELOG.md` — mevcut "N-depth drilldown bundles" girdisine 2 cümle (kimlik dosyadan, malformed sidecar controlled failure).
- Dokunulmadı: `archify/locate/cli.mjs` (çağrı imzası gerekmedi — `validateOwnershipSubset`/`walkOwnershipTree`/`loadSiblingSidecar` orada çağrılmıyor), `archify/test/bundle-validate.test.mjs`/`helpers/bundle-fixture.mjs` (bu turda gerek kalmadı, önceki turdan değişmiş haldeydiler).

---

## Tur 4 (2026-09-12) — astra-4 RET sonrası kök sidecar sertleştirme

Kaynak: `docs/plans/reviews/faz1-astra-4-2026-09-12.md` (hüküm: **RET** — #1/#2/#3 kapanmadan son onay yok). Kapsam yalnız `archify/bundle/diagram-bundle.mjs`, `archify/test/bundle-depth.test.mjs`, `archify/test/bundle-ownership.test.mjs`. Commit/stash/checkout/reset yok. Paralel Faz 2/viewer çalışması nedeniyle **tam suite koşulmadı**, yalnız hedefli komut (aşağıda).

**1. Kök kimliği (tur-3 sapması yanlış gerekçeliydi) — KAPANDI.**
Astra doğru tespit etti: `locate-bundle-delivery.test.mjs:182` custom dosyaya **checkout-platform (entry)** sidecar'ını kopyalıyor, `payments` değil — tur-3'teki "kök kimliği kendi map'inden" sapmasının gerekçesi yanlıştı. `rootSpecPath` artık YENİDEN `manifest.entry`nin spec yolundan türetiliyor (`archify/bundle/diagram-bundle.mjs:774`, `entry.file` html→json), sidecar.map'ten DEĞİL. Bu, tur-3'te eklediğim "kökün kimliği kendi map'i" sapmasını geri aldı. Gerçek sebep tur-3'te farklıydı: `bundle-ownership.test.mjs`'in "unified subset table" testi `payments.ownership.json`'ı doğrudan kök olarak bağlıyordu (entry `checkout-platform` iken) — bunu artık entry-köklü hale getirdim (aşağıya bkz), asıl kırılma nedeni oydu, `locate-bundle-delivery.test.mjs` değil.
Test: `archify/test/bundle-depth.test.mjs:610-655` — `checkout-platform.ownership.json` (doğru adlandırılmış, `manifest.ownership.file` ile doğru bağlanmış) içinde `map:'payments.json'` (gerçek ama yanlış diyagram) → `validateBundle` RET (`bundle/ownership-not-subset: map ... does not resolve to ...'s own diagram spec`).

**2. Sibling'in kendi map'i doğrulanmıyor — KAPANDI.**
`validateOwnershipSubset` içinde `parentSidecarPath` verildiğinde artık `parentSidecar` (yüklenen sibling nesnesi) da kendi `map`i `parentSpecPath`e eşit mi diye kontrol ediliyor (`archify/bundle/diagram-bundle.mjs:176-184`): `resolve(dirname(parentSidecarPath), parentSidecar.map) !== parentSpecPath` → `bundle/ownership-not-subset: ...'s own map ... does not resolve to its expected diagram spec`. Bu kontrol genel (hem kökün kendi sibling'i hem yürünen herhangi bir seviyede `parentSidecar` geçildiğinde çalışır); tur-3'te yalnız kökün KENDİ map'i doğrulanıyordu, kökün EBEVEYNİNİN map'i hiç doğrulanmıyordu.
Test: `archify/test/bundle-depth.test.mjs:656-707` — `checkout-platform.ownership.json` kendi `parent:{map:'payments.json',component:'psp'}`ini doğru bildiriyor ve `payments.ownership.json` diskte var, ama `payments.ownership.json`'ın KENDİ `map`i `'ledger-flow.json'` (yanlış, kendi kimliği `payments.json` olmalı) → RET.

**3. Kökte şema hatası yürüyüşü durdurmuyor — KAPANDI.**
Kök `validateOwnershipSubset` çağrısının sonucu artık `rootFailures` değişkenine alınıyor; boş değilse `note()`lanıp `walkOwnershipTree` hiç çağrılmadan `return` ediliyor (`archify/bundle/diagram-bundle.mjs:775-786`). Önceden (tur-3 sonunda) yalnız ÇOCUK dalı erken duruyordu (`childFailures.length` kontrolü); kökte aynı koruma yoktu, `components:[null]` içeren bir kök sidecar `walkOwnershipTree`nin ilk `component.child_map` erişiminde ham `TypeError` üretiyordu.
Test: `archify/test/bundle-depth.test.mjs:708-745` — kök `components:[null]` → `BundleError` (`bundle/ownership-not-subset`), `TypeError` değil.

**4. (küçük) a→b→c, a→c üçgen regresyonu — KAPANDI.**
İki test eklendi: `archify/test/bundle-depth.test.mjs:774-790` — doğrudan `buildTree` çağrısıyla sentetik `a→b(toB), b→c(toC), a→c(toC)` grafiği, `c.level===2` ve `shared===['c']` doğrulanıyor. `archify/test/bundle-depth.test.mjs:746-773` — asıl eksik olan **build-time** kanıt: derin fixture'ın `checkout-platform.json`'ına `fraud` bileşenine ikinci bir `drilldown:'settlement'` eklenip (gerçek zincir zaten `checkout-platform→payments→settlement`), `buildBundleManifest(dir)` doğrudan çağrılarak `manifest.json` hiç yazılmadan `bundle/drilldown-shared: settlement` ile RET olduğu kanıtlandı.

## Sapma (tur-4)
`archify/test/bundle-ownership.test.mjs`'in "unified subset table through archify bundle --check" testi (brief'te "gerekirse" izinli dosya) `manifest.ownership.file`i doğrudan `payments.ownership.json`a bağlıyordu — madde 1'in "kök kimliği = entry" kuralı altında bu artık geçersiz bir kurulum. `sidecarsFor()`daki ebeveyn bileşenine `child_map:'payments.json'` eklenip kök `checkout-platform.ownership.json`a bağlandı (`archify/test/bundle-ownership.test.mjs:42-49`, `:72-81`) — aynı parent/child glob-subset matrisi artık entry-köklü gerçek bir yürüyüşle sınanıyor, test amacı değişmedi.

## BLOKLAYAN (tur-4)
Yok.

## Kabul (hedefli, tam suite KOŞULMADI — paralel Faz 2/viewer çalışması nedeniyle)
`cd archify && npm run check:validators && node --test test/bundle-*.test.mjs test/locate-bundle*.test.mjs test/generate-validators.test.mjs`
- `check:validators` exit=0.
- `node --test`: **79 test / 79 pass / 0 fail**, exit=0 (tur-3'teki 74'ten +5: 3 kök-sidecar testi + 1 build-time üçgen testi + 1 buildTree üçgen testi; `bundle-ownership.test.mjs` içindeki mevcut testler de dahil, tur-4'ün "unified subset table" düzeltmesiyle birlikte).

## Değişen dosyalar (tur-4)
- `archify/bundle/diagram-bundle.mjs` — `validateOwnershipSubset`e sibling'in kendi map'ini doğrulayan blok eklendi; kök çağrısı `rootSpecPath` yeniden entry'den türetiliyor, sonucu `rootFailures`e alınıp boşsa `walkOwnershipTree`ye hiç girilmiyor.
- `archify/test/bundle-depth.test.mjs` — 5 yeni test (madde 1/2/3/4).
- `archify/test/bundle-ownership.test.mjs` — "unified subset table" testi entry-köklü hale getirildi (madde 1'in yeni kuralına uysun diye).

### Referans testi güncellemesi (Faz 2 sonrası, 2026-09-13)
Faz 2 viewer template'i değiştirdiği için `manifest.reference.json` ile artık yalnız `artifact_sha256` alanlarında ayrışıyordu (render çıktısı değişti, bundle mantığı değil). `manifest.reference.json` DEĞİŞTİRİLMEDİ; test artık (a) yeni manifest'in her `artifact_sha256`'sını diskteki gerçek `.html` dosyasından `htmlArtifactSha256` ile bağımsız doğruluyor, (b) referans metnindeki her diyagramın `artifact_sha256` değerini (yalnız o alan, regex ile) yeni değerle değiştirip diyagram sayısı kadar değişim yapıldığını doğrulayıp geri kalan tüm baytları (`spec_sha256`, ownership sha, boşluk, sıra) maskesiz `Buffer.compare` ile karşılaştırıyor (`archify/test/bundle-depth.test.mjs:53-131`). `node --test test/bundle-depth.test.mjs` → 27/27 pass.
