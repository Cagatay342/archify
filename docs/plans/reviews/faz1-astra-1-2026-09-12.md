- **P1 — Ownership bağı yanlış kabul ediliyor:** `archify/bundle/diagram-bundle.mjs:113–121`, adayları `component` ve ebeveyn dosyasıyla eşleştiriyor; satırın `child` değerini sidecar’ın `map` kimliğiyle bağlamıyor. `b.queue→y` varken `y.parent=a.queue` kabul edilebiliyor. Ayrıca basename karşılaştırması `nested/a.json` ile `a.json` ayrımını kaybediyor.

- **P1 — Bu kontrol torunlara uygulanmıyor:** `archify/bundle/diagram-bundle.mjs:660`, özyinelemeli yürüyüşte yalnız glob altkümesini denetliyor; sidecar şemasını ve manifest ebeveyn bağını doğrulamıyor. Bellek içi tam-validator denemesinde yanlış torun ebeveyni **`ok:true`, 10/10** verdi. Her kenarda gerçek `(parent, component, child)` bağı doğrulanmalı.

- **P1 — Bozuk sidecar kontrollü hata yerine çöküyor:** `archify/bundle/diagram-bundle.mjs:669` üzerinden `null` sidecar’a girildiğinde `:644` doğrudan `current.components` okuyor; **TypeError** doğrulandı. Şema kontrolü başarısızsa o dal kesilmeli.

- **P2 — Derinlik sınırı geç uygulanıyor:** `archify/bundle/diagram-bundle.mjs:220`, sınırı denetlemeden özyinelemeye devam ediyor; `:362/:493` kontrolüne ulaşamadan 10.000 düğümlü zincirde **RangeError** oluştu. İteratif dolaşım veya erken derinlik kontrolü gerekli.

- **Graph ayrıntısı:** `archify/bundle/diagram-bundle.mjs:195` BFS değil DFS. Geçerli ağaçta seviyeler doğru; shared grafikte ilk keşfedilen yol kullanılıyor. `a→b→c, a→c` için `c.level=2` hesaplanıyor. `:224` yalnız entry’den gezdiğinden erişilemeyen cycle/shared yapıları yalnız orphan olarak raporlanıyor; geçersiz bundle yine reddediliyor.

- **Doğru görünenler:** `archify/bundle/diagram-bundle.mjs:298`, entry bileşen sırasını, ardından ebeveynlerin id sırasını koruyor. `:668–670` visited ekleme/silme işlemleri aktif ata yolunu doğru tutuyor. `archify/locate/cli.mjs:339–360`, doğrulanmış seviyelerle ebeveyni önce işleyip effective ownership üzerinden exclusions zincirini doğru biriktiriyor.

- **Byte-aynılık kanıtı eksik:** `archify/test/bundle-depth.test.mjs:52`, yalnız derinlik, diyagram sayısı ve doğrulamayı denetliyor; önceki manifest ile alan veya byte karşılaştırmıyor. Kod aynı renderer/template girdileriyle geçerli iki seviyeli ağaçların sırasını koruyor, fakat istenen regresyon testi yok.

- **Yanlış geçen pozitif test:** `archify/test/bundle-depth.test.mjs:205–215`, `components:[]` nedeniyle şemadan zaten hata alıyor; yalnız “ambiguous/does not name” yokluğunu sınadığı için geçiyor. Geçerli sidecar kullanıp `failures` değerini `[]` ile karşılaştırmalı.

- **Negatifler ilgili kodu tetikliyor:** `archify/test/bundle-depth.test.mjs:21`, hedef hata kodunu özellikle arıyor; ek `embed-mismatch` hataları tek başına testi geçiremez. `:157` ise yalnız şema reddini kanıtlıyor; brief buna izin verse de gerçek dokuz seviyeli graph dalı ve sekiz seviyenin kabulü sınanmıyor.

- **Locate kapsam boşluğu:** `archify/test/locate-bundle-depth.test.mjs:94`, üç seviyede zaten ebeveyn-önce gelen satırlarla çalışıyor; seviye sıralaması kaldırılırsa bunu yakalamaz. Dört seviyeli, id sırası bağımlılık sırasını bozan fixture ve ara ebeveyne özgü exclusion gerekli. Sidecar’lar bundle üretildikten sonra eklendiğinden bu test manifest ownership yürüyüşünü de kapsamıyor.

- **Docs:** `archify/references/drilldown-bundles.md:6` hâlâ toplam “up to twelve … children” diyor; sınır ebeveyn başına açıklanmalı. Derinlik örneği, yeni/kaldırılan kodlar ve locate zinciri mevcut. Plan §7’deki toplam bayt bütçesi uygulanmamış; brief bunu zorunlu maddeye dönüştürmemiş.

- **Faz 2 manifest alanları eksik değil:** `max_depth`, `level`, kimlikler ve kenarlar mevcut; `subtree/selfId/depth` mesaj sözleşmesi Faz 2 kapsamında ve mevcut alanlardan üretilebilir.

- **Kabul kanıtı tamamlanmamış:** `docs/plans/reports/faz1-bundle-depth-2026-09-12.md:43`, ZIP hatasını “aksiyon gerekmez” sayıyor; baseline istisnalarında bu hata yok. Hedefli 65/65 sonucu, brief’in tam-suite kabul koşulunu karşılamıyor.

- **İnceleme sınırı:** Dosya yazmadım; Git/ağ kullanmadım. Suite çalıştırılmadı. Salt okunur self-map kontrolü baseline ile aynı **ownership-stale, 9/10**; validator güncellik kontrolü kopyada `ajv` bulunmadığından tamamlanamadı.

- **Hüküm: DÜZELTMEYLE KABUL — Ownership bağlama ve bozuk-girdi hataları giderilip eksik regresyon/kabul kanıtları tamamlanmadan Faz 1 kabul edilmemeli.**