**Hüküm: DÜZELTMEYLE KABUL. P1 maddeleri Faz 3’ten önce kapanmalı.** İnceleme salt okunurdu; `check:viewer` geçti. Kaynaktan alınan fonksiyonlarla bellek içi kontroller yaptım; Chrome koşusunu tekrarlamadım.

`V` = [viewer/template.source.html](viewer/template.source.html), `T` = [drilldown-nested-browser.test.mjs](archify/test/drilldown-nested-browser.test.mjs).

§7 kapanış durumu:

- **İki alıcı yolu KAPANDI:** doğrudan ebeveyn ve çocuk ayrı işleniyor; ara viewer ikisini de alabiliyor (`V:6972`).
- **Gezinme oturumu AÇIK:** sayaç var; fakat ACK/escape/crumb session’sız kabul ediliyor, ascend-request/done session denetlemiyor (`V:6763`, `V:6935`, `V:6948`, `V:6964`).
- **Readiness=ACK KAPANDI:** `open` yalnız başarılı handshake’te yazılıyor (`V:6734`). Ancak 170 ms sonra `descending` de kaldırılıyor; belgelenen durum dizisi tam korunmuyor (`V:6818`).
- **Kök breadcrumb zinciri AÇIK:** normal toplama/çizim tamam; yinelenen ACK zinciri sıfırlıyor, kapanmış oturumdan crumb kabulü mümkün (`V:6701`, `V:6753`, `V:6958`).
- **Esc merdiveni AÇIK:** normal akış ve node’a odak dönüşü mevcut; zincir ve settle yarışları tek-seviye garantisini bozuyor (`V:6853`, `V:6891`, `V:7134`).
- Diğer §7 maddeleri: listener kurulumu ve yerel/mutlak durum ayrımı **KAPANDI** (`V:7005`, `V:6586`, `V:4400`); subtree kenar eşleştirmesi **AÇIK**: `drillFor()` yalnız component karşılaştırıyor, `(parent,component,child)` ve `max_depth` kontrolü yok (`V:6547`, `V:6756`).

Güvenlik değerlendirmesi:

- `event.source` kontrolü yabancı pencereyi ve torunun köke doğrudan mesajını reddediyor; **`event.origin` kontrolü yok**, gönderimler `'*'` (`V:6972`). Bu, `file:` uyumluluğuyla tutarlı; HTTP’de aynı iframe’in başka origin’e gezinmesine karşı origin sabitlemesi sağlamıyor.
- Doğru kaynak pencereden gelen sahte mesajlar bütünüyle engellenmiyor: session’sız ACK/crumb, eski session’lı ascend-done kabulünü bellek içi kontrolde doğruladım. Crumb metni `textContent` ile çiziliyor; burada HTML enjeksiyonu görmedim (`V:6683`).
- Subtree yalnız yüzeysel doğrulanıp ebeveynden kabul ediliyor (`V:6906`). SHA kontrolü **bildirilen kimlik/digest eşitliği**; HTML/spec baytları yeniden hash’lenmiyor. Güvenilmeyen ebeveyn/çocuk bunu aşabilir; `artifact_sha256` zaten çevrimdışı doğrulama sözleşmesidir ([drilldown-bundles.md:155](archify/references/drilldown-bundles.md:155)).

Gerekli düzeltmeler:

- **P1 — Session ve durum kapıları:** bütün gezinme mesajlarında zorunlu, tam eşleşen session; ascend yanıtlarında ayrıca istek eşleştirmesi uygulanmalı. `ascending`/`stale`/kapalı durumda ACK ve crumb reddedilmeli; yinelenen ACK zinciri silmemeli (`V:6944`). `back()` sırasında `current` 170 ms korunuyor; bu aralıkta geç ACK iframe’i yeniden görünür yapabiliyor (`V:6825`, `V:6746`).
- **P1 — Settle yaşam döngüsü:** `back()` yalnız callback referansını siliyor; 200 ms timer çalışmaya devam ediyor. Eski isteğin callback’inin yeniden açılan **session 2’yi kapattığını** doğruladım (`V:6830`, `V:6874`). Timer iptali ve session/istek kontrolü gerekli. Normal yanıt yolunda iç iframe önce kaldırılıyor; her katta bağımsız başlayan 200 ms fallback, yavaş alt seviyede bu sırayı garanti etmiyor.
- **P2 — Subtree/readiness:** kenar üçlüsü, derinlik ve subtree şekli doğrulanmalı; animasyon bitişi `descending` durumundan ayrılmalı (`V:6547`, `V:6906`, `V:6820`).
- **P2 — Test kanıtı:** gecikmiş/eksik session, yinelenen ACK, back→yeniden descend→eski settle, kök rung ile toplu ascend, kardeş geçişi ve daha derin zincir eksik. `T:280` yalnız yeni açılış tamamlandıktan sonraki eski sayısal ACK’i sınar.
- **P2 — Klavye/file:** sentetik document keydown handler’ı sınar; gerçek odak yönlendirmesini kanıtlamaz (`T:185`). “Yalnız CDP kısıtı” hükmü erken. Kök testinde gerçek breadcrumb odağı/assert’i yok (`T:234`); üç seviyeli `file://` akışı da HTTP’ye geçilerek kapsam dışında kalmış.

Stale kartı ara viewer’da gösteriliyor; kök zinciri ara seviyeyi ve başarısız hedef rung’unu koruyor (`V:6712`). Test yalnız ara seviye adının bulunmasını doğruluyor; zincirin tamamını ve stale sonrası Esc’i sınamıyor (`T:273`).

**Üretilmiş dosyaları Faz 5’e bırakmak makul**, fakat yeniden üretim ve tüm kapıların geçmesi yayın önkoşulu olmalı. Dört fail + 12 golden açıklaması tutarlı; atıf yapılan `faz2-tests.log` bu kopyada yok, dolayısıyla hepsinin yalnız bayat baytlardan kaynaklandığını bağımsız doğrulayamadım. Bu erteleme yukarıdaki P1’leri kapsamıyor.